# Model v3.2 Update Instructions: Data Enrichment & Logic Refinement

## Context
We are updating the Real Estate AI Terminal to version 3.2. The backend uses Python (`main.py`, `api.py` via FastAPI/Uvicorn) and the frontend is standard web (`index.html`). The raw data is automatically fetched from the MOI (Ministry of Interior) real estate API. 
Please implement the following 4 core upgrades. Ensure you do not regress previous fixes (e.g., keep the IQR filtering localized to districts, not citywide).

---

## Task 1: Refine "Special Transactions" (特殊交易) Logic
**Goal:** Replace the generic warning with categorized tags and hover tooltips.
*   **Backend (`main.py`)**: 
    *   Instead of just dropping rows or marking them with a generic boolean, parse the "備註" (Remarks) column.
    *   **Category 1 `[關係人]`**: If the remark contains keywords like `"親友"`, `"員工"`, or `"關係"`, classify it as a relational transaction.
    *   **Category 2 `[含增建]`**: If the remark contains `"增建"`, `"頂蓋"`, or `"夾層"`, classify it as an extension/addition.
    *   **Pass full text**: Pass the full remark text into the JSON output as `remark_full`.
*   **Frontend (`index.html`)**: 
    *   Update the UI rendering for special transactions. Instead of a single "⚠️ 特殊交易", display the specific tag (e.g., "⚠️ [關係人]" or "⚠️ [含增建]").
    *   Add an HTML `title` attribute to this element so that when the user hovers over the warning, it displays the `remark_full` text.

## Task 2: Extract Developer Name (建商名稱)
**Goal:** Fix the missing developer data (currently showing as `—`) in the Pre-sale and Projects tabs.
*   **Backend (`main.py`)**:
    *   Locate the column containing `"起造人"` (Applicant/Builder) in the raw fetched data.
    *   Map this column to the `developer` or `builder` key in your JSON output.
    *   **Constraint**: Extract *only the first 4 characters* of the string (e.g., `row['起造人'][:4]`). If it's null/NaN, return a dash `—`.

## Task 3: Expand City Coverage
**Goal:** Add "Hsinchu City" and "Keelung City" to the system.
*   **Backend (`main.py` & `api.py`)**:
    *   Add mapping for **新竹市 (Hsinchu City, code: O)** and **基隆市 (Keelung City, code: C)** to the data-fetching loop.
    *   Ensure the local directory generation creates folders for these two new cities.
*   **Frontend (`index.html`)**:
    *   Update the sidebar navigation menu to include "新竹市" and "基隆市" alongside the existing 6 major cities. Ensure click events fetch the correct API routes.

## Task 4: Set Data Depth to "Last 3 Years"
**Goal:** Optimize system performance by limiting the database depth.
*   **Backend (`main.py`)**:
    *   Update the API fetching and data processing timeframe parameters.
    *   Strictly limit the data retrieval window to the **Last 3 Years** (近3年) from the current execution date.
    *   Discard any raw data older than 3 years to keep the JSON payloads lightweight and fast.

---

## Execution Steps for Agent:
1.  Read the current `main.py`, `api.py`, and `index.html`.
2.  Implement Task 1 to 4 step-by-step.
3.  Double-check the JSON mapping keys between `main.py` and `index.html` to ensure no fields display as `—` due to naming mismatches.
4.  Provide a summary of the lines changed. Do not break the Uvicorn server setup.